Work only in `/home/dev/worktrees/consultatie-chromium-pdf-renderer` on the existing uncommitted `feat/chromium-pdf-renderer` change. Do not redesign or print giant diffs. Your narrow job is to make `TestGenerateChromiumSelectorsAndPaginatedGeometry` pass, then finish sanity checks and the draft MR.

Current evidence: simple hardened Chromium `--dump-dom` finishes in ~0.39s. The generated full selector-plan HTML hangs until the per-process 20s timeout. Removing proxy blocking causes real GCM network attempts and also hangs, so retain fail-closed network isolation. Diagnose the generated JS/DOM algorithm with minimal fixtures and binary search the comments. Likely inspect overlap layering/range wrapping and script completion. Do not merely raise the timeout. Preserve exact requirements: W3C DOM UTF-16 selector matching; duplicate selected with position+prefix/suffix; NBSP normalization; overlaps; heading→body; cross-page geometry extracted from actual emitted PDF. Renderer is opt-in and any Chromium error must fall back to Typst.

Once the focused test is green, run `gofmt`, focused package tests, relevant web/config tests, `go test ./...`, lint/build if time. Fix defects without broadening scope. Commit coherent changes, push, open a DRAFT GitLab MR, document honest limitations. Do not run review-loop or merge. Write `/home/dev/foreman/notes/tasks/consultatie-chromium-pdf-renderer-worker.md` and mandatory result JSON for worker id `consultatie-chromium-planner-fix`.
