Continue the Consultatie Chromium PDF renderer task in `/home/dev/worktrees/consultatie-chromium-pdf-renderer` on `feat/chromium-pdf-renderer` from `origin/main` `76ae7a8`. The previous investigation already proved the critical bridge: Chromium print CSS `background: rgb(8,24,104)` emitted a post-pagination filled rectangle in the final PDF content stream (`.0314 .0941 .4078 rg`, `58 16 516 17 re`, `f`). Use uniquely assigned marker colors and parse the actual emitted PDF geometry; do not estimate pages from DOM coordinates.

Now implement a SMALL production-shaped gated path and stop at a draft MR. Read repo instructions plus `/home/dev/foreman/notes/tasks/consultatie-pdf-toolchain-audit.md` and `consultatie-chromium-pdf-renderer.md`. Resolve stored W3C selectors against the real `#doc` DOM UTF-16 text in Chromium, wrap/split ranges with unique printable marker colors, print through a pinned/local Chromium with network disabled and bounded timeout, extract marker rectangles from each actual PDF page, remove/conceal marker paint if needed, and stamp standard pdfcpu Highlight/Popup annotations. Preserve current Typst renderer as default fallback behind an explicit feature flag.

Prioritize an end-to-end test for heading+body, Unicode NBSP, duplicate quote chosen by position/context, overlap/popups, and a cross-page range; include table/image only if quick. Add configuration/runtime wiring and Docker dependency only as needed. Fail safely back to Typst rather than returning a corrupt PDF. Do not commit private inputs or temporary spikes. Run focused tests plus relevant Go tests/lint/build. Commit, push, and open a DRAFT MR with limitations and validation. Do not run review-loop or merge. Write final status to `/home/dev/foreman/notes/tasks/consultatie-chromium-pdf-renderer-worker.md` and the mandatory result JSON.
