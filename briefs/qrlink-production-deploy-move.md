Work only in `/home/dev/qrlink-deploy-move` on branch `fix/production-deploy-host` for edits. You may read `/home/dev/qrlink-server` and remote history/config, but do not edit or push the server repo.

Henk says qrlink production moved to one dedicated server at `95.216.21.184`; whitelabel is now on that same server, and the new Docker Compose/deployment contract is in the `qrlink/server` repo. Diagnose the current qrlink production deploy workflow and update it to deploy both normal and whitelabel production correctly to the new unified host using the actual current server-repo compose contract. Do not merely replace two IP strings: verify paths, service/project names, image/tag inputs, secrets, SSH actions, sequencing/concurrency, and whether the old two-job split should be consolidated or retained. Preserve security and avoid exposing secrets. Add or update low-cost validation if practical, run focused syntax/workflow checks, and document the exact safe release path given that rerunning existing tag `4.24.7` uses its old tagged workflow definition.

Commit and push a small reviewable change, then open a DRAFT PR targeting master with a clear summary, validation, rollout note, and no automatic issue closure unless an actual matching issue exists. Do not merge, do not tag, do not deploy production, and do not run the expensive review-loop. If the current server contract or needed secret/key identity cannot be determined safely, report the exact blocker instead of guessing. Finish with PR URL, commit SHA, tests/checks, exact deployment behavior, required secrets/host setup, and recommended release steps.
