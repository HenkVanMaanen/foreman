Resume the active qrlink CI repair in /home/dev/qrlink on branch fix/687-qr-search-in-actions.

The working tree already contains uncommitted edits from a worker that hit its session limit. Preserve and audit them; do not discard or blindly redo them. The observed failures are ERR_PNPM_IGNORED_BUILDS in cms, website, qrgenereren, and web-scan Docker builds after pnpm 10 behavior changed. The intended direction is narrow per-project pnpm build-script allowlists, not blanket disabling scripts. Current edits add pnpm-workspace.yaml files and update Dockerfiles to copy them. Audit every allow/deny entry against package scripts and lockfiles, especially vue3-snackbar (expected deny because it only runs an only-allow guard). Also address the underlying floating pnpm version if safely in scope: these projects lack packageManager pins while their Dockerfiles use bare corepack enable. Choose and justify a compatible explicit pnpm version based on their lockfiles/tooling; keep project pins consistent where appropriate, but do not guess if compatibility cannot be established.

Read /home/dev/qrlink/AGENTS.md and CLAUDE.md fully before editing. Inspect the existing diff first. Run meaningful clean-install/build verification for every affected package, preferably matching Docker CI. Do not run the expensive review-loop. Commit with the repository's configured identity and push the existing branch to origin (henk explicitly requested fix all CI errors and deploy; branch pushes are authorized). Do not merge, deploy, un-draft, close issues, or touch prod. Return a concise result in the worker result.json contract, including exact tests, commit SHA, remaining non-code failures, and any deployment decision still requiring henk.
