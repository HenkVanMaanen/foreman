Update existing draft MR !60 in `/home/dev/worktrees/consultatie-chromium-pdf-renderer`, branch `feat/chromium-pdf-renderer`, per Henk msg 333051168: make the Chromium PDF renderer default-enabled so the preview actually exercises it and, if approved, production will use it by default. Keep Typst as automatic runtime fallback on any Chromium failure.

Inspect current config/Helm/Docker wiring and choose a clear backward-compatible override to disable Chromium if needed. Update defaults, config tests, chart values/templates/tests and documentation/comments as appropriate. Ensure the container's Chromium binary path matches the default config. Add/adjust a web/config test proving default uses Chromium configuration and explicit false disables it if the config parser supports that distinction. Do not weaken renderer tests or fallback behavior.

Run focused config/web/pdfexport tests, `go test ./...`, lint/build (quick sanity). Commit and push directly to the existing branch/MR !60. Do not open another MR, undraft, run review-loop, or merge. Update `/home/dev/foreman/notes/tasks/consultatie-chromium-pdf-renderer-worker.md` with the new commit and checks, and write the mandatory result JSON for id `consultatie-chromium-default-enabled`.
