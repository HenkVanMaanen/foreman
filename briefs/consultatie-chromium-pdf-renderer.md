You are the implementation worker for a new Consultatie PDF renderer. Work only in `/home/dev/worktrees/consultatie-chromium-pdf-renderer` on branch `feat/chromium-pdf-renderer`, based on the just-merged `origin/main`.

Read the repository guidance and inspect the current complete-export pipeline. Also read `/home/dev/foreman/notes/tasks/consultatie-pdf-toolchain-audit.md` and `/home/dev/foreman/notes/tasks/consultatie-chromium-pdf-renderer.md` for requirements and prior research.

Build the smallest production-shaped, reviewable implementation of a gated Chromium/browser-native render path that materially replaces the fragile Typst text-projection placement. The same DOM/layout used for printing should resolve W3C selectors in UTF-16 coordinates; annotation geometry must reflect actual PDF pagination, not naive `getClientRects()/A4-height` arithmetic. Reuse pdfcpu for standard Highlight/Popup annotations if appropriate, and retain the existing Typst route as a safe fallback/feature flag while the new path is evaluated.

First investigate the repo and prove a robust post-pagination geometry bridge. Then implement it with focused tests. Cover at minimum headings/body spans, cross-page selection, overlaps/popups, duplicate quotes with position/context, Unicode whitespace, and representative tables/images where architecture permits. Keep browser network disabled/pinned and resource use bounded. Do not commit private PDFs, CSVs, annotations, or customer text from `/tmp`.

Run targeted tests plus the relevant full Go tests/lint/build. Benchmark enough to detect an obvious regression, but do not spend hours optimizing before correctness. Commit in small coherent commits, push the branch, and open a DRAFT GitLab MR with an honest description of what is implemented, validation, feature flag/fallback, and known limitations. Do not run `review-loop`, undraft, merge, or modify unrelated code. Write progress/final status to `/home/dev/foreman/notes/tasks/consultatie-chromium-pdf-renderer-worker.md`, including MR URL, commit SHA, commands/results, and blockers.
