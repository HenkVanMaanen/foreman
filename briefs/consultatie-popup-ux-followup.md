You are a fresh Codex senior engineer auditing and, if necessary, correcting draft MR !59 in worktree /home/dev/worktrees/consultatie-complete-export-placement-audit on branch fix/complete-export-comment-placement.

Problem: Henk says the expected VNG popup is not visible for CSV annotation 01a04005-13d2-76a9-8321-7804ba88dc82. The first follow-up commit 8355fc8 merges overlap-connected comments and sorts by broadest highlight. Its claimed Chromium proof is /tmp/consultatie-evidence/chromium-fixed-overlap-click.png, but visual inspection shows the popup starts with ActiZ, not VNG; VNG is buried. Do not accept the previous worker's claim at face value.

Independently inspect the real private proof PDF/data under /tmp/consultatie-evidence and /tmp/consultatie-complete-export-20260904 (never commit private content, names, IDs, or fixtures). Determine exactly which annotation wins the click at the earlier overlapping part of the VNG selection and why the visible popup begins with ActiZ. Assess whether overlap-connected components cause unsafe transitive merging of comments that do not directly share the clicked region.

Design the narrowest robust PDF UX fix so clicking the problematic overlapping yellow text makes the relevant overlapping remarks immediately discoverable, without silently hiding other comments or merging unrelated chain-connected annotations. Preserve IDs, geometry, highlight appearance, popup-parent links, and existing behavior for non-overlapping highlights. Prefer direct spatial overlap/region semantics over global connected-component propagation. Add privacy-safe synthetic regression tests that would fail 8355fc8 for the real failure mode, including transitive A-B-C overlap where A and C do not overlap. Produce an actual Chromium screenshot proving the visible popup at the problematic click includes VNG immediately, not merely somewhere below the scroll viewport.

Remove all private repro test files before committing. Run focused tests, full race tests, lint/nilaway, and build. Push corrective commit(s) to the existing branch/MR, keep MR draft, do not run review-loop and do not merge. Update notes/tasks/consultatie-complete-export-placement-audit-worker.md with evidence and write the standard result JSON.
