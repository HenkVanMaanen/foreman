Fresh Codex architecture spike, read-only unless a small throwaway prototype outside the repo is useful. Repository/worktree: /home/dev/worktrees/consultatie-complete-export-placement-audit. Henk asks whether PDF comments/highlights should use other tooling instead of the current Pandoc/Typst two-pass text matching because real comments still go missing or overlap ambiguously.

Independently map the current export pipeline and evaluate practical alternatives available to this Go service, with an evidence-based prototype on the private source/CSV under /tmp/consultatie-complete-export-20260904 (never commit or expose private content). Priority hypothesis: render the same browser DOM via headless Chromium `page.pdf`, derive exact selector rectangles from DOM Range/getClientRects using stored UTF-16 positions plus quote/context validation, transform CSS coordinates to PDF points/pages, then stamp standard PDF Highlight/Popup annotations. Also assess Playwright/Puppeteer, wkhtmltopdf/weasyprint, LibreOffice/docx, pdfcpu/unidoc/borb-style stamping, and whether Chromium can natively emit annotations. Consider headers/footers, page breaks, fonts, links, accessibility, determinism, security, performance at 692/1000 comments, deployment image size, licensing, and matching stale selectors.

Produce a concrete recommendation: stay/fix current pipeline, hybrid migration, or replace. Include a minimal private proof if feasible that locates the newly reported VNG selector correctly. Do not modify/push product code or MR in this architecture task. Record a concise decision memo in notes/tasks/consultatie-pdf-toolchain-audit.md and result JSON.
